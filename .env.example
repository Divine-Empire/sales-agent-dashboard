# Render backend base URL
SALES_AGENT_API_URL=https://your-service.onrender.com
# Shared secret sent as X-API-Key. Must match SALES_AGENT_API_KEY on the backend.
# Server-side only — never NEXT_PUBLIC_.
SALES_AGENT_API_KEY=
